{
     % tests conditional expn
     var a : Integer
     var b, c : Boolean

     a := 1
     b := true

     % S30 condition not boolean
     a := ((a / 1) ? 1 : 2)
     
     % S33 expressions after condition not same type
     a := ((b) ? 1 : b)

     % S24 return type assigned to other types
     a := ((b) ? true : false)
}