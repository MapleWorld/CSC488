% Test and expression
{
    var k : Boolean
    k := true
    
    if k and false then write "fail"
    else write "pass"

    if k and true then write "pass"
    else write "fail"

    if false and k then write "fail"
    else write "pass"

    if false and false then write "fail"
    else write "pass"
}