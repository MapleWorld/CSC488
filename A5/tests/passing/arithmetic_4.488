% Arithmetic expression precedence of Operators
{
    var x, y : Integer
    x := 1 * 2 - 3 * 4              % Result 10
    y := (1 * 2) - (3 * 4)          % Result 10
    write x, y                      % 1010
}
