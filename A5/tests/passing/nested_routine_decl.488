{
    function a : Integer {
        procedure b (c : Integer, d : Boolean) {
            var a : Boolean
            procedure f {
                 function g (k : Integer, g : Boolean) : Boolean {
                     var c : Integer
                     c := 1
                     if (c = 1) then {
                          write "hi"
                     } else {
                           var f : Integer
                           f := 1
                     }
                     return with true
                 }
                 a := true
                 return
             }
             a := true
             return
        }
        return with 1
     }

    write "hi"
}